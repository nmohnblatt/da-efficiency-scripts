[project]
name = "da-efficiency-scripts"
version = "0.1.0"
description = "This repository contains Python scripts to compute efficiency metrics of data availability sampling schemes."
requires-python = ">=3.12"
dependencies = [
    "tabulate>=0.10.0",
]
